INFORMATIONS
========

`/config/initializers/bson/object_id.rb` is useless when using older version of Rails.
Because of [this issue from doorkeeper](https://github.com/applicake/doorkeeper/issues/307), we need it for Rails4.
